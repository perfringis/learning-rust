// Control Flow
// Repetition with Loops

// Repeating Code with loop

// we could stop the infinite execution of loop by providing break keyword
// we could omit and jump to next iteration using continue keyword

fn main() {
    loop {
        println!("again!");
    }
}
